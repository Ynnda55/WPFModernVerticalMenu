<UserControl x:Class="WPFModernVerticalMenu.Views.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFModernVerticalMenu.Views"
             xmlns:vm="clr-namespace:WPFModernVerticalMenu.ViewsModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:VMHome />
    </UserControl.DataContext>
    <Grid>
        <Border CornerRadius="5" Background="{DynamicResource SecundaryBackgroundColor}">
            <StackPanel>
                <Button Height="50" Width="200" Command="{Binding Ejemplo}">Prueba Boton Linkeado</Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
